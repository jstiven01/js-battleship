const UI = (() => {

  const renderInitialBoards = (player, gameBoard) => {
    for (let i = 0; i < gameBoard.getBoard().lenght; i +=1) {
      for (let j = 0; j < gameBoard.getBoard().lenght; j +=1) {
        
      }
    }
  }
})();

export default UI;